## UI Improvement Task: Filter Panel Redesign

The current **filter panel UI looks visually inconsistent and needs to be updated**.

### Goal
Refactor the existing filter panel so that it **matches the design shown in the attached image**.

### Requirements
- **Pixel-accurate implementation** of the attached design.
- Preserve **all existing styling rules, design tokens, and theme variables** (colors, spacing, typography, shadows, etc.).
- Do **not introduce new styles** unless absolutely necessary — prefer reusing existing classes and variables.
- Ensure the updated UI remains **fully responsive** and consistent with the rest of the application.

### Constraints
- Do not break existing functionality (filters must continue to work as before).
- Avoid large structural rewrites unless required for layout alignment.
- Keep the code **clean and maintainable**.

### Expected Outcome
- Filter panel visually matches the provided design.
- No regressions in behavior or styling consistency across the app.

### Attachments

[MOCKUP](@Task1/thoughts/mockups/FILTER/Screenshot 2026-05-01 at 14.20.29.png)